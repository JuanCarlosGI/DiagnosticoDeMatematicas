@using DiagnosticoDeMatematicas.Helpers

@{
    ViewBag.Title = "Exams";
}

@Html.Action("Evaluations")

@if (Request.IsAuthenticated)
{
    if (User.IsInRole("Admininstrator"))
    {
        @Html.Action("ApprovedEvaluations")
    }
}